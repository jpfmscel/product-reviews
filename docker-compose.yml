version: "3"
services:
  auth-service:
    build: ./auth-service/
    ports:
      - 8081:8080
    environment:
      MONGO_URI: 'mongodb://mongo/test'
    links:
      - mongo           
  product-service:
    build: ./product-service/
    environment: 
      - "MONGO_URI:mongodb://mongo/test"
    ports:
      - 8082:8080
    links:
      - mongo
  review-service:
    build: ./review-service/
    environment: 
      - "MONGO_URI:mongodb://mongo/test"
    ports:
      - 8083:8080
    links:
      - mongo
  mongo:
    image: mongo:3.6.3-jessie
    ports:
      - 27017:27017
    volumes:
      - mongo:/data/db
volumes: 
  mongo: 
